# Módulos Novos 11–13 (Dicionários/Tuplas, Strings, Arquivos/CSV) — Plano

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar três módulos novos — 11 Dicionários e tuplas, 12 Strings, 13 Arquivos e dados (CSV) — seguindo o molde validado (módulo 06) e os módulos 08–10, e registrá-los na trilha.

**Architecture:** Cada módulo é um fragmento HTML em `licoes/`, registrado em `js/lessons.js`. Reaproveita a infra do `main`: matplotlib sob demanda do playground e o CSS didático. NENHUM código JS/CSS novo. Arquivos (módulo 13) usam o sistema de arquivos virtual em memória do Pyodide — `open(...)` funciona no navegador; cada exemplo ESCREVE o arquivo e depois LÊ (a memória é reiniciada a cada execução).

**Tech Stack:** HTML fragmento + SVG inline; Python no navegador via Pyodide; matplotlib sob demanda; stdlib `math` e o FS virtual do Pyodide. Sem numpy (fica para o módulo 14).

## Global Constraints

- **Molde canônico = `licoes/06-lacos-de-repeticao.html`**; referências adicionais já no padrão: `licoes/08-funcoes-e-modulos.html`, `licoes/09-vetores-e-listas.html`, `licoes/10-recursividade.html`. Ler o molde antes de escrever.
- **Estrutura obrigatória por módulo (mesma dos módulos 08–10), nesta ordem:** (1) `<h1>`; (2) `.analogia` (`.analogia-icone` + analogia de engenharia); (3) `<figure class="ilustracao">` com `<svg>` inline theme-aware (cores via `var(--cor-*)`/`currentColor`; `<defs>` de marcador só se houver setas; `role="img"` + `aria-label`; `<figcaption>`); (4) explicação progressiva com exemplos executáveis curtos + um `.callout`; (5) `<h2>Na engenharia</h2>` + `.eng-grid` com 3 `.eng-card` (`.eng-mec` ⚙️, `.eng-ele` ⚡, `.eng-tel` 📡, cada um com `.eng-selo` e um `.exemplo`); (6) `<section class="exercicios">` com **exatamente 15 exercícios**: 5 `.nivel-facil` (🟢 Aquecimento) + 6 `.nivel-medio` (🟡 Praticando) + 4 `.nivel-dificil` (🔴 Desafio de engenharia), numerados 1–15.
- **Exercício** = `<div class="exercicio nivel-XXX">` com `<span class="exercicio-faixa">🟢/🟡/🔴 Rótulo</span>`, enunciado `<p><strong>N.</strong> …</p>` e `<details><summary>Mostrar solução</summary> … </details>`.
- **Os 4 🔴** têm `.solucao-dupla` com dois `.solucao-bloco`: `<span class="solucao-rotulo">Sem gráfico (texto)</span>` e `<span class="solucao-rotulo">Com gráfico (matplotlib)</span>`. O bloco gráfico tem `data-mpl` no `.exemplo`, código com o literal `import matplotlib.pyplot as plt` e termina com `plt.show()`.
- **Markup de exemplo executável** idêntico ao módulo 06: `<div class="exemplo">` → `<div class="exemplo-barra"><span>Python</span><button class="btn btn-executar">▶ Executar</button></div>` → `<pre><code>…</code></pre>`.
- **Linha de referência em gráficos** (quando houver `axhline`) usa `linestyle="--"` (consistência com o molde).
- **Escape em `<pre><code>`:** `<`/`>` crus como no módulo 06; nunca usar entidades para eles. Cuidado: nunca deixar `<` colado a uma letra dentro de `<code>` (viraria tag).
- **Pré-requisitos de conteúdo** (ordem da trilha): 1–10 já existem (até recursividade). Logo, estes módulos PODEM usar variáveis, operadores, condicionais, laços, funções, listas e recursão. Strings básicas (f-strings, `split`) já apareceram informalmente; o módulo 12 as formaliza.
- **Correção:** todo Python sintaticamente válido e com o resultado pretendido (sem runner — raciocinar). Soluções matplotlib: montar dados e `plt.plot`/`plt.bar` + `title`/`xlabel`/`ylabel` + `plt.show()`.
- **Arquivos (módulo 13):** usar o FS virtual do Pyodide. Cada exemplo é autossuficiente: primeiro escreve (`with open("dados.txt", "w") as f: ...`), depois lê. Não depender de arquivos pré-existentes.
- **Commit** termina com: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- **Verificação:** estática. Navegador é portão do usuário no fim.

---

### Task 1: Módulo 11 — Dicionários e tuplas

**Files:** Create: `licoes/11-dicionarios-e-tuplas.html`

**Interfaces:** Consumes molde 06 + módulos 08–10; CSS didático; matplotlib do playground. Produces fragmento da lição 11. Id de rota (Task 4): `dicionarios-e-tuplas`.

**Design:**
- **Analogia (⚡):** um dicionário é uma *tabela de consulta* — você procura por uma chave (o código de cor de um resistor, o nome de um material) e obtém o valor associado, sem percorrer tudo. Uma tupla é um *par fixo* (uma coordenada `(x, y)`, um par tensão/corrente) que não muda.
- **Ilustração SVG:** uma "tabela de consulta" — caixas `chave → valor` empilhadas (ex.: `"prata" → 10%`, `"ouro" → 5%`); ao lado, uma tupla `(3, 4)` como um par fixo.
- **Explicação progressiva (exemplos executáveis):** (a) criar `aluno = {"nome": "Ana", "idade": 20}` e acessar `aluno["nome"]`; (b) adicionar/atualizar `aluno["curso"] = "Eng"`; (c) percorrer `for chave in d` e `for chave, valor in d.items()`; (d) `len(d)`; (e) tupla `ponto = (3, 4)` e desempacotar `x, y = ponto`. `.callout`: acessar chave inexistente dá erro (`KeyError`); tupla é imutável.
- **Na engenharia (3 cards):**
  - ⚡ Elétrica: `cores = {"preto": 0, "marrom": 1, "vermelho": 2}` ; traduzir uma cor: `print(cores["vermelho"])`.
  - ⚙️ Mecânica: `densidades = {"aço": 7850, "alumínio": 2700, "cobre": 8960}` ; imprimir a do alumínio.
  - 📡 Telecom: `canais = {1: 2412, 6: 2437, 11: 2462}` (Wi-Fi 2,4 GHz, MHz) ; imprimir a frequência do canal 6.

**Blueprint dos 15 exercícios (enunciado + solução de referência):**

🟢 (5):
1. Criar `aluno = {"nome": "Bia", "idade": 22}` e imprimir o nome. — `aluno = {"nome": "Bia", "idade": 22}` / `print(aluno["nome"])`
2. Dado `precos = {"cafe": 5, "pao": 3}`, imprimir os dois preços. — `precos = {"cafe": 5, "pao": 3}` / `print(precos["cafe"])` / `print(precos["pao"])`
3. Criar um dict vazio, adicionar duas chaves e imprimir o dict. — `d = {}` / `d["a"] = 1` / `d["b"] = 2` / `print(d)`
4. Criar a tupla `ponto = (3, 4)` e imprimir cada coordenada. — `ponto = (3, 4)` / `print(ponto[0])` / `print(ponto[1])`
5. Percorrer `notas = {"mat": 8, "fis": 9}` imprimindo chave e valor. — `notas = {"mat": 8, "fis": 9}` / `for materia, nota in notas.items():` / `    print(materia, nota)`
🟡 (6):
6. Somar todos os valores de `precos = {"a": 10, "b": 20, "c": 30}`. — `precos = {"a": 10, "b": 20, "c": 30}` / `s = 0` / `for v in precos.values():` / `    s += v` / `print(s)`  (esperado 60)
7. Encontrar a maior nota em `notas = {"ana": 7, "leo": 9, "mia": 8}` (valor). — `notas = {"ana": 7, "leo": 9, "mia": 8}` / `maior = 0` / `for v in notas.values():` / `    if v > maior:` / `        maior = v` / `print(maior)`  (esperado 9)
8. Imprimir cada item de `estoque = {"parafuso": 100, "porca": 80}` no formato `chave: valor`. — `estoque = {"parafuso": 100, "porca": 80}` / `for k, v in estoque.items():` / `    print(f"{k}: {v}")`
9. Desempacotar a tupla `medida = (12.5, "V")` em `valor` e `unidade` e imprimir juntos. — `medida = (12.5, "V")` / `valor, unidade = medida` / `print(f"{valor} {unidade}")`
10. Contar pares e ímpares de 1 a 10 usando um dict contador. — `contagem = {"par": 0, "impar": 0}` / `for n in range(1, 11):` / `    if n % 2 == 0:` / `        contagem["par"] += 1` / `    else:` / `        contagem["impar"] += 1` / `print(contagem)`  (esperado {'par': 5, 'impar': 5})
11. Dado `cores = {"marrom": 1, "vermelho": 2, "laranja": 3}`, traduzir uma lista de cores em dígitos. — `cores = {"marrom": 1, "vermelho": 2, "laranja": 3}` / `faixas = ["vermelho", "laranja", "marrom"]` / `for c in faixas:` / `    print(c, "->", cores[c])`
🔴 (4) — solução dupla:
12. (⚡ Elétrica) `medicoes = {0: 0.0, 1: 1.8, 2: 3.0, 3: 3.7, 4: 4.1}` (tempo→tensão). Texto imprime cada par; gráfico plota tensão × tempo.
   - Texto: `medicoes = {0: 0.0, 1: 1.8, 2: 3.0, 3: 3.7, 4: 4.1}` / `for t, v in medicoes.items():` / `    print(f"t={t}s  v={v} V")`
   - Gráfico: `import matplotlib.pyplot as plt` / `medicoes = {0: 0.0, 1: 1.8, 2: 3.0, 3: 3.7, 4: 4.1}` / `tempos = list(medicoes.keys())` / `tensoes = list(medicoes.values())` / `plt.plot(tempos, tensoes, marker="o")` / `plt.title("Medições")` / `plt.xlabel("t (s)")` / `plt.ylabel("V")` / `plt.grid(True)` / `plt.show()`
13. (⚙️ Mecânica) `densidades = {"aço": 7850, "alumínio": 2700, "cobre": 8960, "titânio": 4500}`. Texto imprime cada material; gráfico de barras das densidades.
   - Texto: `densidades = {"aço": 7850, "alumínio": 2700, "cobre": 8960, "titânio": 4500}` / `for material, d in densidades.items():` / `    print(f"{material}: {d} kg/m³")`
   - Gráfico: `import matplotlib.pyplot as plt` / `densidades = {"aço": 7850, "alumínio": 2700, "cobre": 8960, "titânio": 4500}` / `materiais = list(densidades.keys())` / `valores = list(densidades.values())` / `plt.bar(materiais, valores)` / `plt.title("Densidade dos materiais")` / `plt.ylabel("kg/m³")` / `plt.show()`
14. (📡 Telecom) `canais = {1: 2412, 6: 2437, 11: 2462}` (canal Wi-Fi → MHz). Texto imprime cada canal; gráfico plota frequência × canal.
   - Texto: `canais = {1: 2412, 6: 2437, 11: 2462}` / `for c, f in canais.items():` / `    print(f"canal {c}: {f} MHz")`
   - Gráfico: `import matplotlib.pyplot as plt` / `canais = {1: 2412, 6: 2437, 11: 2462}` / `cs = list(canais.keys())` / `fs = list(canais.values())` / `plt.plot(cs, fs, marker="s")` / `plt.title("Canais Wi-Fi 2,4 GHz")` / `plt.xlabel("canal")` / `plt.ylabel("MHz")` / `plt.grid(True)` / `plt.show()`
15. (geral) Histograma: contar quantas vezes cada dígito aparece nos números de 1 a 20 (apenas o dígito das unidades) com um dict, e plotar em barras.
   - Texto: `contagem = {}` / `for n in range(1, 21):` / `    d = n % 10` / `    contagem[d] = contagem.get(d, 0) + 1` / `for d in sorted(contagem):` / `    print(d, contagem[d])`
   - Gráfico: `import matplotlib.pyplot as plt` / `contagem = {}` / `for n in range(1, 21):` / `    d = n % 10` / `    contagem[d] = contagem.get(d, 0) + 1` / `digitos = sorted(contagem)` / `freq = [contagem[d] for d in digitos]` / `plt.bar(digitos, freq)` / `plt.title("Frequência do dígito das unidades (1–20)")` / `plt.xlabel("dígito")` / `plt.ylabel("ocorrências")` / `plt.show()`

- [ ] **Step 1: Ler o molde** (`licoes/06-lacos-de-repeticao.html`) e um dos módulos 08–10 para referência.
- [ ] **Step 2: Escrever `licoes/11-dicionarios-e-tuplas.html`** com `<h1>Dicionários e tuplas</h1>` e toda a estrutura obrigatória (analogia ⚡, SVG da tabela de consulta, explicação progressiva, 3 cards, 15 exercícios; 4 🔴 com solução dupla).
- [ ] **Step 3: Autorrevisão estática** — 15 exercícios (5/6/4), numeração 1–15; 4 🔴 com `.solucao-dupla` + `data-mpl`/`import matplotlib`/`plt.show()`; resultados conferidos (6→60, 7→9, 10→{par:5,impar:5}); `.get(d, 0)` correto; classes batem; Python relido.
- [ ] **Step 4: Commit**
```bash
git add licoes/11-dicionarios-e-tuplas.html
git commit -m "feat: módulo 11 — Dicionários e tuplas (molde de engenharia)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Módulo 12 — Strings

**Files:** Create: `licoes/12-strings.html`

**Interfaces:** Consumes molde 06 + módulos 08–11; CSS didático; matplotlib. Produces fragmento da lição 12. Id de rota: `strings`.

**Design:**
- **Analogia (📡):** uma string é uma *sequência de caracteres* — como uma mensagem de texto transmitida, caractere por caractere, cada um numa posição. Manipular texto é fatiar, juntar e transformar essa sequência.
- **Ilustração SVG:** os caracteres de `"OLA"` (ou `"SINAL"`) em caixas indexadas com índices `0,1,2…` por baixo — paralelo direto ao vetor indexado do módulo 09, mas com letras.
- **Explicação progressiva (exemplos executáveis):** (a) `s = "Engenharia"`, `len(s)`, `s[0]`, `s[-1]`; (b) fatiar `s[0:3]`; (c) métodos `.upper()`, `.lower()`, `.strip()`, `.replace("a", "@")`; (d) `.split()` em uma frase; (e) percorrer `for letra in s`. `.callout`: strings são imutáveis — métodos retornam uma nova string, não alteram a original.
- **Na engenharia (3 cards):**
  - 📡 Telecom: contar caracteres de uma mensagem: `msg = "ola mundo"` ; `print(len(msg))`.
  - ⚙️ Mecânica: padronizar um código de peça: `codigo = "  pe-204 "` ; `print(codigo.strip().upper())`.
  - ⚡ Elétrica: extrair valores de `"12;5;9"` com `split(";")` e somar como inteiros.

**Blueprint dos 15 exercícios:**

🟢 (5):
1. Dado `s = "Python"`, imprimir o tamanho. — `s = "Python"` / `print(len(s))`  (esperado 6)
2. Imprimir o primeiro e o último caractere de `"sinal"`. — `s = "sinal"` / `print(s[0])` / `print(s[-1])`
3. Converter `"engenharia"` para maiúsculas e minúsculas. — `s = "engenharia"` / `print(s.upper())` / `print(s.lower())`
4. Concatenar nome e sobrenome com um espaço. — `nome = "Ada"` / `sobrenome = "Lovelace"` / `print(nome + " " + sobrenome)`
5. Usar f-string para mostrar um número com 2 casas. — `x = 3.14159` / `print(f"{x:.2f}")`  (esperado 3.14)
🟡 (6):
6. Contar as vogais de `"telecomunicacoes"`. — `s = "telecomunicacoes"` / `vogais = 0` / `for letra in s:` / `    if letra in "aeiou":` / `        vogais += 1` / `print(vogais)`  (esperado 8)
7. Inverter a string `"abcde"`. — `s = "abcde"` / `print(s[::-1])`  (esperado edcba)
8. Dividir `"a,b,c,d"` por vírgula e contar os pedaços. — `s = "a,b,c,d"` / `partes = s.split(",")` / `print(len(partes))`  (esperado 4)
9. Substituir todos os espaços de `"a b c"` por `-`. — `s = "a b c"` / `print(s.replace(" ", "-"))`  (esperado a-b-c)
10. Verificar se `"arara"` é palíndromo. — `s = "arara"` / `print(s == s[::-1])`  (esperado True)
11. Limpar e padronizar `"  Motor-AC  "` (tirar espaços, maiúsculas). — `s = "  Motor-AC  "` / `print(s.strip().upper())`  (esperado MOTOR-AC)
🔴 (4) — solução dupla:
12. (📡 Telecom) Frequência de cada letra de uma mensagem usando um dict; texto imprime, gráfico em barras.
   - Texto: `msg = "telecomunicacoes"` / `freq = {}` / `for letra in msg:` / `    freq[letra] = freq.get(letra, 0) + 1` / `for letra in sorted(freq):` / `    print(letra, freq[letra])`
   - Gráfico: `import matplotlib.pyplot as plt` / `msg = "telecomunicacoes"` / `freq = {}` / `for letra in msg:` / `    freq[letra] = freq.get(letra, 0) + 1` / `letras = sorted(freq)` / `valores = [freq[l] for l in letras]` / `plt.bar(letras, valores)` / `plt.title("Frequência de letras")` / `plt.xlabel("letra")` / `plt.ylabel("ocorrências")` / `plt.show()`
13. (⚡ Elétrica) Uma string `"0;2;5;9;12"` com amostras separadas por `;`. Converter para números e plotar.
   - Texto: `dados = "0;2;5;9;12"` / `partes = dados.split(";")` / `for p in partes:` / `    print(int(p))`
   - Gráfico: `import matplotlib.pyplot as plt` / `dados = "0;2;5;9;12"` / `valores = [int(p) for p in dados.split(";")]` / `plt.plot(valores, marker="o")` / `plt.title("Amostras de uma string")` / `plt.xlabel("índice")` / `plt.ylabel("valor")` / `plt.grid(True)` / `plt.show()`
14. (⚙️ Mecânica) Lista de códigos de peça; contar o comprimento de cada código e plotar.
   - Texto: `codigos = ["PE-1", "MOT-22", "ENG-103", "X9"]` / `for c in codigos:` / `    print(c, len(c))`
   - Gráfico: `import matplotlib.pyplot as plt` / `codigos = ["PE-1", "MOT-22", "ENG-103", "X9"]` / `comprimentos = [len(c) for c in codigos]` / `plt.bar(codigos, comprimentos)` / `plt.title("Tamanho dos códigos")` / `plt.ylabel("nº de caracteres")` / `plt.show()`
15. (geral) Contar palavras por comprimento numa frase e plotar (quantas palavras têm 1, 2, 3… letras).
   - Texto: `frase = "o rato roeu a roupa do rei de roma"` / `contagem = {}` / `for palavra in frase.split():` / `    n = len(palavra)` / `    contagem[n] = contagem.get(n, 0) + 1` / `for n in sorted(contagem):` / `    print(f"{n} letras: {contagem[n]} palavra(s)")`
   - Gráfico: `import matplotlib.pyplot as plt` / `frase = "o rato roeu a roupa do rei de roma"` / `contagem = {}` / `for palavra in frase.split():` / `    n = len(palavra)` / `    contagem[n] = contagem.get(n, 0) + 1` / `tamanhos = sorted(contagem)` / `qtd = [contagem[n] for n in tamanhos]` / `plt.bar(tamanhos, qtd)` / `plt.title("Palavras por comprimento")` / `plt.xlabel("nº de letras")` / `plt.ylabel("nº de palavras")` / `plt.show()`

- [ ] **Step 1: Ler o molde** e os módulos 09/11 (vetor indexado / uso de dict) para referência.
- [ ] **Step 2: Escrever `licoes/12-strings.html`** com `<h1>Strings</h1>` e a estrutura obrigatória (analogia 📡, SVG de caracteres indexados, explicação progressiva, 3 cards, 15 exercícios; 4 🔴 com solução dupla).
- [ ] **Step 3: Autorrevisão estática** — 15 (5/6/4), numeração 1–15; 4 🔴 com solução dupla + matplotlib; resultados conferidos (1→6, 5→3.14, 6→8, 7→edcba, 8→4, 9→a-b-c, 10→True, 11→MOTOR-AC); classes batem; `<`/`>` não aparecem colados a letras em `<code>` (strings não usam `<`); Python relido.
- [ ] **Step 4: Commit**
```bash
git add licoes/12-strings.html
git commit -m "feat: módulo 12 — Strings (molde de engenharia)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Módulo 13 — Arquivos e dados (CSV)

**Files:** Create: `licoes/13-arquivos-e-dados.html`

**Interfaces:** Consumes molde 06 + módulos 08–12; CSS didático; matplotlib. Produces fragmento da lição 13. Id de rota: `arquivos-e-dados`.

**Design:**
- **Analogia (⚙️):** um arquivo é um *caderno de dados* — um log onde você grava medições para usar depois. CSV (valores separados por vírgula) é o formato mais comum de planilha/medição em engenharia.
- **Nota técnica no texto:** explicar de forma amigável que, aqui no navegador, o arquivo é gravado numa "memória temporária" (some ao recarregar) — por isso cada exemplo primeiro **grava** e depois **lê** o mesmo arquivo.
- **Ilustração SVG:** um "arquivo" (documento) com linhas de dados (`t,v` / `0,0.0` / `1,1.8` …), sugerindo linhas e colunas.
- **Explicação progressiva (exemplos executáveis):** (a) escrever um arquivo com `with open("dados.txt", "w") as f: f.write(...)` e depois ler com `with open("dados.txt") as f: print(f.read())`; (b) escrever várias linhas num laço; (c) ler linha por linha com `for linha in f`; (d) CSV: escrever cabeçalho + linhas e ler separando com `split(",")`. `.callout`: sempre usar `with open(...)` (fecha o arquivo sozinho); aqui a "memória" é temporária.
- **Na engenharia (3 cards):**
  - ⚙️ Mecânica: gravar 3 medições de força num arquivo e ler de volta.
  - ⚡ Elétrica: gravar um CSV `tempo,tensao` e somar a coluna de tensão.
  - 📡 Telecom: gravar amostras num arquivo e contar quantas linhas tem.

**Blueprint dos 15 exercícios:** (cada solução é autossuficiente: grava e depois lê)

🟢 (5):
1. Gravar `"Olá, arquivo!"` em `nota.txt` e ler de volta. — `with open("nota.txt", "w") as f:` / `    f.write("Olá, arquivo!")` / `with open("nota.txt") as f:` / `    print(f.read())`
2. Gravar os números 1 a 5 (um por linha) num arquivo. — `with open("nums.txt", "w") as f:` / `    for n in range(1, 6):` / `        f.write(f"{n}\n")` / `with open("nums.txt") as f:` / `    print(f.read())`
3. Ler um arquivo linha por linha e imprimir cada linha sem o `\n`. — `with open("nums.txt", "w") as f:` / `    f.write("10\n20\n30\n")` / `with open("nums.txt") as f:` / `    for linha in f:` / `        print(linha.strip())`
4. Contar quantas linhas tem um arquivo. — `with open("dados.txt", "w") as f:` / `    f.write("a\nb\nc\nd\n")` / `with open("dados.txt") as f:` / `    linhas = f.readlines()` / `print(len(linhas))`  (esperado 4)
5. Acrescentar (modo "a") uma linha a um arquivo já criado e ler tudo. — `with open("log.txt", "w") as f:` / `    f.write("linha 1\n")` / `with open("log.txt", "a") as f:` / `    f.write("linha 2\n")` / `with open("log.txt") as f:` / `    print(f.read())`
🟡 (6):
6. Gravar um CSV com cabeçalho `id,valor` e 3 linhas, depois ler e imprimir. — `with open("d.csv", "w") as f:` / `    f.write("id,valor\n")` / `    f.write("1,10\n")` / `    f.write("2,20\n")` / `    f.write("3,30\n")` / `with open("d.csv") as f:` / `    print(f.read())`
7. Separar uma linha CSV `"1,10,laranja"` com `split(",")` e imprimir os campos. — `linha = "1,10,laranja"` / `campos = linha.split(",")` / `print(campos)`
8. Ler um CSV (sem cabeçalho) e somar a segunda coluna. — `with open("d.csv", "w") as f:` / `    f.write("1,10\n2,20\n3,30\n")` / `soma = 0` / `with open("d.csv") as f:` / `    for linha in f:` / `        partes = linha.split(",")` / `        soma += int(partes[1])` / `print(soma)`  (esperado 60)
9. Calcular a média de uma coluna de um CSV. — `with open("m.csv", "w") as f:` / `    f.write("0,2\n1,4\n2,6\n")` / `valores = []` / `with open("m.csv") as f:` / `    for linha in f:` / `        valores.append(float(linha.split(",")[1]))` / `print(sum(valores) / len(valores))`  (esperado 4.0)
10. Ler um CSV e imprimir só as linhas cuja segunda coluna é maior que 15. — `with open("d.csv", "w") as f:` / `    f.write("1,10\n2,20\n3,30\n")` / `with open("d.csv") as f:` / `    for linha in f:` / `        partes = linha.split(",")` / `        if int(partes[1]) > 15:` / `            print(linha.strip())`
11. Gerar um CSV `n,quadrado` para n de 1 a 5 e ler de volta. — `with open("q.csv", "w") as f:` / `    f.write("n,quadrado\n")` / `    for n in range(1, 6):` / `        f.write(f"{n},{n*n}\n")` / `with open("q.csv") as f:` / `    print(f.read())`
🔴 (4) — solução dupla:
12. (⚡ Elétrica) Gravar um CSV `tempo,tensao` (0..5, tensão de carga), ler, e plotar tensão × tempo.
   - Texto: `import math` / `with open("rc.csv", "w") as f:` / `    f.write("tempo,tensao\n")` / `    for n in range(6):` / `        t = n * 1.0` / `        v = 5 * (1 - math.exp(-t))` / `        f.write(f"{t},{v:.3f}\n")` / `with open("rc.csv") as f:` / `    print(f.read())`
   - Gráfico: `import matplotlib.pyplot as plt` / `ts = []` / `vs = []` / `with open("rc.csv") as f:` / `    next(f)` / `    for linha in f:` / `        a, b = linha.split(",")` / `        ts.append(float(a))` / `        vs.append(float(b))` / `plt.plot(ts, vs, marker="o")` / `plt.title("Tensão lida do CSV")` / `plt.xlabel("t (s)")` / `plt.ylabel("V")` / `plt.grid(True)` / `plt.show()`
   (Observação: o bloco gráfico assume que `rc.csv` foi gravado; para ser autossuficiente, repetir a gravação no início do bloco gráfico antes da leitura.)
13. (⚙️ Mecânica) Gravar medições de força `n,forca`, ler e plotar.
   - Texto: `with open("f.csv", "w") as f:` / `    f.write("amostra,forca\n")` / `    forcas = [12, 18, 25, 30, 28, 22]` / `    for i in range(len(forcas)):` / `        f.write(f"{i},{forcas[i]}\n")` / `with open("f.csv") as f:` / `    print(f.read())`
   - Gráfico: `import matplotlib.pyplot as plt` / `with open("f.csv", "w") as f:` / `    f.write("amostra,forca\n")` / `    forcas = [12, 18, 25, 30, 28, 22]` / `    for i in range(len(forcas)):` / `        f.write(f"{i},{forcas[i]}\n")` / `xs = []` / `ys = []` / `with open("f.csv") as f:` / `    next(f)` / `    for linha in f:` / `        a, b = linha.split(",")` / `        xs.append(int(a))` / `        ys.append(float(b))` / `plt.plot(xs, ys, marker="s")` / `plt.title("Força (lida do CSV)")` / `plt.xlabel("amostra")` / `plt.ylabel("força (N)")` / `plt.grid(True)` / `plt.show()`
14. (📡 Telecom) Gravar amostras de uma senoide num CSV, ler e plotar a forma de onda.
   - Texto: `import math` / `with open("sinal.csv", "w") as f:` / `    f.write("t,amostra\n")` / `    for n in range(21):` / `        t = n * 0.05` / `        f.write(f"{t:.2f},{math.sin(2*math.pi*t):.4f}\n")` / `with open("sinal.csv") as f:` / `    print(f.read())`
   - Gráfico: `import math` / `import matplotlib.pyplot as plt` / `with open("sinal.csv", "w") as f:` / `    f.write("t,amostra\n")` / `    for n in range(21):` / `        t = n * 0.05` / `        f.write(f"{t:.2f},{math.sin(2*math.pi*t):.4f}\n")` / `ts = []` / `ys = []` / `with open("sinal.csv") as f:` / `    next(f)` / `    for linha in f:` / `        a, b = linha.split(",")` / `        ts.append(float(a))` / `        ys.append(float(b))` / `plt.plot(ts, ys)` / `plt.title("Sinal lido do CSV")` / `plt.xlabel("t (s)")` / `plt.ylabel("amplitude")` / `plt.grid(True)` / `plt.show()`
15. (geral) Gravar um CSV de medições, ler, e plotar os valores junto da sua média (`axhline` tracejada).
   - Texto: `valores = [10, 14, 9, 16, 12, 18, 11]` / `with open("med.csv", "w") as f:` / `    for v in valores:` / `        f.write(f"{v}\n")` / `lidos = []` / `with open("med.csv") as f:` / `    for linha in f:` / `        lidos.append(int(linha.strip()))` / `print("média:", sum(lidos) / len(lidos))`
   - Gráfico: `import matplotlib.pyplot as plt` / `valores = [10, 14, 9, 16, 12, 18, 11]` / `with open("med.csv", "w") as f:` / `    for v in valores:` / `        f.write(f"{v}\n")` / `lidos = []` / `with open("med.csv") as f:` / `    for linha in f:` / `        lidos.append(int(linha.strip()))` / `media = sum(lidos) / len(lidos)` / `plt.plot(lidos, marker="o", label="medições")` / `plt.axhline(media, color="red", linestyle="--", label="média")` / `plt.title("Medições lidas do CSV")` / `plt.xlabel("índice")` / `plt.ylabel("valor")` / `plt.legend()` / `plt.grid(True)` / `plt.show()`

- [ ] **Step 1: Ler o molde** e o módulo 09/12 para referência.
- [ ] **Step 2: Escrever `licoes/13-arquivos-e-dados.html`** com `<h1>Arquivos e dados</h1>` e a estrutura obrigatória (analogia ⚙️, nota sobre memória temporária, SVG do arquivo, explicação progressiva, 3 cards, 15 exercícios; 4 🔴 com solução dupla). IMPORTANTE: cada bloco gráfico 🔴 deve ser autossuficiente — GRAVAR o CSV no início do próprio bloco antes de ler (ver itens 13–15; ajustar o item 12 para também gravar no bloco gráfico).
- [ ] **Step 3: Autorrevisão estática** — 15 (5/6/4), numeração 1–15; 4 🔴 com solução dupla + matplotlib autossuficiente (grava antes de ler); resultados conferidos (4→4, 8→60, 9→4.0); `with open(...)` em todo acesso; uso de `\n` correto; `next(f)` pula cabeçalho onde há cabeçalho; classes batem; Python relido.
- [ ] **Step 4: Commit**
```bash
git add licoes/13-arquivos-e-dados.html
git commit -m "feat: módulo 13 — Arquivos e dados / CSV (molde de engenharia)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Registrar os três módulos na trilha

**Files:** Modify: `js/lessons.js` (acrescentar 3 entradas após `recursividade`)

**Interfaces:** Consumes os arquivos das Tasks 1–3 e seus ids (`dicionarios-e-tuplas`, `strings`, `arquivos-e-dados`). Produces as lições no menu/rotas.

- [ ] **Step 1: Acrescentar as entradas** — em `js/lessons.js`, após o objeto `recursividade` (ordem 10) e antes do `];`:
```javascript
  {
    id: "dicionarios-e-tuplas",
    ordem: 11,
    titulo: "Dicionários e tuplas",
    arquivo: "licoes/11-dicionarios-e-tuplas.html",
  },
  {
    id: "strings",
    ordem: 12,
    titulo: "Strings",
    arquivo: "licoes/12-strings.html",
  },
  {
    id: "arquivos-e-dados",
    ordem: 13,
    titulo: "Arquivos e dados",
    arquivo: "licoes/13-arquivos-e-dados.html",
  },
```
- [ ] **Step 2: Autorrevisão estática** — três objetos com vírgulas corretas, `ordem` 11/12/13, `arquivo` apontando para os arquivos criados, `id` batendo com os ids de rota. Array válido.
- [ ] **Step 3: Commit**
```bash
git add js/lessons.js
git commit -m "feat: registra módulos 11-13 na trilha de lições

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review (autor do plano)

**Cobertura:** molde replicado (estrutura/analogia/SVG/3 cards/15 exercícios 5/6/4/4 duplas) → Tasks 1–3 via Global Constraints + blueprints. Módulos novos do spec (11 dicionários/tuplas, 12 strings, 13 arquivos/CSV) → Tasks 1–3. ⚙️/⚡/📡 → cards + 🔴 por área. Registro → Task 4. ✔

**Placeholder scan:** cada um dos 45 exercícios tem enunciado + solução de referência exata. O item 12 do módulo 13 traz observação explícita para tornar o bloco gráfico autossuficiente (gravar antes de ler), e o Step 2/3 reforçam. Sem "TBD". ✔

**Consistência de nomes:** ids de rota e caminhos idênticos entre Tasks 1–3 (Produces) e Task 4. Classes/markup iguais aos módulos 06/08–10 já no `main`. `axhline` com `linestyle="--"` (item 13.15) conforme o molde. Pré-requisitos respeitados (dict→`.get`, strings, arquivos usam só o que já foi ensinado + FS virtual). ✔

**Escopo:** módulo 14 (NumPy — exige extensão do playground) e enriquecimento de 01–05/07 ficam para planos seguintes.

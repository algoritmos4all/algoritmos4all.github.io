# Enriquecer Módulo 07 (Juntando tudo) — Plano

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Enriquecer a lição capstone 07 (Juntando tudo) com analogia de engenharia, ilustração SVG, seção "Na engenharia" e 12 exercícios graduados — incluindo, por ser pós-laços, **soluções duplas (com e sem gráfico matplotlib)** nos 3 desafios 🔴 — PRESERVANDO os 3 mini-projetos existentes.

**Architecture:** Fragmento HTML em `licoes/07-juntando-tudo.html` (já registrado). Reaproveita CSS didático, SVG inline e o playground (matplotlib sob demanda). Sem código JS/CSS novo.

**Tech Stack:** HTML fragmento + SVG inline; Pyodide; matplotlib sob demanda.

## Global Constraints

- **PRESERVAR o conteúdo existente**: os 3 mini-projetos (Adivinhe o número, Calculadora de média com situação, Soma de uma lista de compras), seus callouts, a seção "E agora?" e o callout final. Absorver o "Desafio final" (caixa eletrônico) no conjunto graduado.
- **Molde de referência = `licoes/06-lacos-de-repeticao.html`** (molde COMPLETO, com soluções duplas) e os módulos 04/05 recém-enriquecidos para o padrão de analogia/SVG/cards. Classes CSS já existem.
- **Estrutura, nesta ordem:** (1) `<h1>Juntando tudo</h1>` (manter); (2) `.analogia` (`.analogia-icone` + analogia) — NOVO; (3) `<figure class="ilustracao">` com `<svg>` theme-aware (`var(--cor-*)`/`currentColor`, SEM fallback `#hex`; `role="img"`+`aria-label`; `<figcaption>`; `<defs>` se houver setas) — NOVO; (4) os 3 mini-projetos existentes PRESERVADOS (parabéns + projetos 1/2/3 + callouts); (5) `<h2>Na engenharia</h2>` + `.eng-grid` com 3 `.eng-card` (`.eng-mec` ⚙️, `.eng-ele` ⚡, `.eng-tel` 📡, cada um com `.eng-selo` e um `.exemplo` que combina laço+condicional) — NOVO; (6) `<section class="exercicios">` com 12 exercícios (4 `.nivel-facil` 🟢 + 5 `.nivel-medio` 🟡 + 3 `.nivel-dificil` 🔴), numerados 1–12 — EXPANDIR (absorver o "caixa eletrônico"); os 3 🔴 têm `.solucao-dupla`; (7) PRESERVAR a seção "E agora?" e o callout final (após os exercícios ou antes deles — manter o sentido de fechamento; recomendado: manter "E agora?"/callout final DEPOIS dos exercícios como conclusão da trilha).
- **Rótulos de faixa e frase de abertura idênticos ao molde:** 🟢 Aquecimento / 🟡 Praticando / 🔴 Desafio de engenharia; "Faça na ordem. 🟢 aquecem a sintaxe, 🟡 combinam ideias, 🔴 aplicam à engenharia. Tente antes de abrir a solução."
- **Os 3 🔴** têm `<details>` com `.solucao-dupla` contendo dois `.solucao-bloco`: `<span class="solucao-rotulo">Sem gráfico (texto)</span>` e `<span class="solucao-rotulo">Com gráfico (matplotlib)</span>`. O bloco gráfico tem `data-mpl`, código com `import matplotlib.pyplot as plt` e `plt.show()`.
- **Markup de exemplo** idêntico ao molde.
- **Escape em `<pre><code>`:** `<`/`>` crus (com espaço, ex.: `while t <= 2.0:`); nunca `<` colado a letra.
- **Disciplina de pré-requisitos:** o módulo 07 vem DEPOIS de laços (06) e ANTES de funções/listas (08/09). Pode usar: variáveis, input, conversão, operadores, condicionais (`if/elif/else`), laços (`for`/`while`, `range`, acumulador, `break`), `import random`, `import math`. **NÃO usar funções (`def`), nem listas como estrutura de dados ensinada.** EXCEÇÃO PRÁTICA: para montar os dados de um gráfico, os blocos "Com gráfico" criam uma lista vazia e usam `.append()` num laço (`xs = []` / `for ...: xs.append(...)`). Isso é o mínimo necessário para plotar; incluir UMA nota curta no texto da lição dizendo que "listas guardam vários valores — você vê em detalhe no módulo 9; aqui usamos só para juntar os pontos do gráfico". Um laço sobre uma lista literal (`for nota in [100, 50, 20, 10]:`) também é permitido com nota de "prévia" (o arquivo atual já faz isso).
- **`axhline`** (quando usado) com `linestyle="--"`.
- **Correção:** todo Python válido e com resultado pretendido; laços terminam; `break` onde necessário. Sem runner — raciocinar.
- **Commit** termina com: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- **Verificação:** estática; navegador é portão do usuário.

---

### Task 1: Enriquecer Módulo 07 — Juntando tudo

**Files:** Modify: `licoes/07-juntando-tudo.html`

**Design:**
- **Analogia (⚙️):** "juntando tudo" é como *montar um instrumento completo* a partir das peças: variáveis guardam as leituras, condicionais tomam decisões e laços repetem o processo — juntos formam uma rotina de medição e controle de verdade, como um sistema de monitoramento.
- **Ilustração SVG:** blocos rotulados `variáveis` + `condicionais` + `laços` (três caixas) convergindo com setas para uma caixa maior `programa completo`. Theme-aware, com `<defs>` de seta.
- **Na engenharia (3 cards):** cada card é um mini-programa que combina laço + condicional:
  - ⚙️ Mecânica: simular queda e avisar quando passa de 20 m — `t = 0.0` / `while t <= 3.0:` / `    altura = 0.5 * 9.81 * t**2` / `    if altura > 20:` / `        print(f"t={t:.1f}s passou de 20 m")` / `    t = t + 0.5`.
  - ⚡ Elétrica: varrer 5 leituras e contar quantas excedem 230 V — `leituras = [228, 235, 240, 225, 250]` / `acima = 0` / `for v in leituras:` / `    if v > 230:` / `        acima = acima + 1` / `print(f"{acima} leituras acima de 230 V")` (nota: lista literal como prévia).
  - 📡 Telecom: amostrar um sinal e marcar amostras positivas — `import math` / `for n in range(8):` / `    s = math.sin(2 * math.pi * n / 8)` / `    if s > 0:` / `        print(f"amostra {n}: positiva")`.

**Exercícios (12) — manter o desafio atual e expandir. Enunciado + solução:**
🟢 (4):
1. Mostrar os números de 1 a 10 com um `for`. — `for n in range(1, 11):` / `    print(n)`
2. Contagem regressiva de 5 a 1 com `while` e depois "Já!". — `n = 5` / `while n >= 1:` / `    print(n)` / `    n = n - 1` / `print("Já!")`
3. Somar os números de 1 a 50 com acumulador. — `soma = 0` / `for n in range(1, 51):` / `    soma = soma + n` / `print(soma)`  (1275)
4. Mostrar só os pares de 1 a 20 (laço + `if`). — `for n in range(1, 21):` / `    if n % 2 == 0:` / `        print(n)`
🟡 (5):
5. (absorver o atual) Caixa eletrônico: notas de 100/50/20/10 para um saque (laço sobre lista literal + `//` e `%`). — (solução atual do "Desafio final")
6. Tabuada de um número lido, de 1 a 10. — `n = int(input("Número: "))` / `for i in range(1, 11):` / `    print(f"{n} x {i} = {n * i}")`
7. Contar quantos múltiplos de 3 há de 1 a 100. — `c = 0` / `for n in range(1, 101):` / `    if n % 3 == 0:` / `        c = c + 1` / `print(c)`  (33)
8. Ler 5 números e mostrar o maior (laço + `if`). — `maior = float(input("Número: "))` / `for _ in range(4):` / `    x = float(input("Número: "))` / `    if x > maior:` / `        maior = x` / `print(f"Maior: {maior}")`
9. Para cada número de 1 a 10, dizer se é par ou ímpar (laço + `if/else`). — `for n in range(1, 11):` / `    if n % 2 == 0:` / `        print(f"{n}: par")` / `    else:` / `        print(f"{n}: ímpar")`
🔴 (3 — Desafio de engenharia, solução dupla; o bloco gráfico monta os pontos com laço + `append`):
10. (⚙️ Mecânica) Simular a queda livre de 0 a 3 s (passo 0,2 s); no texto, imprimir cada altura e avisar quando passa de 20 m; no gráfico, plotar a trajetória.
   - Texto: `g = 9.81` / `t = 0.0` / `while t <= 3.0:` / `    altura = 0.5 * g * t**2` / `    if altura > 20:` / `        print(f"t={t:.1f}s  altura={altura:.1f} m (passou de 20 m)")` / `    else:` / `        print(f"t={t:.1f}s  altura={altura:.1f} m")` / `    t = t + 0.2`
   - Gráfico: `import matplotlib.pyplot as plt` / `g = 9.81` / `ts = []` / `ys = []` / `t = 0.0` / `while t <= 3.0:` / `    ts.append(t)` / `    ys.append(0.5 * g * t**2)` / `    t = t + 0.2` / `plt.plot(ts, ys, marker="o")` / `plt.axhline(20, color="red", linestyle="--")` / `plt.title("Queda livre")` / `plt.xlabel("t (s)")` / `plt.ylabel("altura (m)")` / `plt.grid(True)` / `plt.show()`
11. (⚡ Elétrica) Monitorar 10 leituras de tensão (use `random`); no texto, contar quantas passam de 230 V; no gráfico, plotar as leituras com a linha de limite.
   - Texto: `import random` / `acima = 0` / `for n in range(10):` / `    v = random.randint(210, 250)` / `    if v > 230:` / `        acima = acima + 1` / `    print(f"leitura {n}: {v} V")` / `print(f"{acima} leituras acima de 230 V")`
   - Gráfico: `import random` / `import matplotlib.pyplot as plt` / `xs = []` / `vs = []` / `for n in range(10):` / `    xs.append(n)` / `    vs.append(random.randint(210, 250))` / `plt.plot(xs, vs, marker="o")` / `plt.axhline(230, color="red", linestyle="--")` / `plt.title("Monitor de tensão")` / `plt.xlabel("leitura")` / `plt.ylabel("tensão (V)")` / `plt.grid(True)` / `plt.show()`
12. (📡 Telecom) Amostrar uma senoide de 0 a 1 s (passo 0,05 s); no texto, contar quantas amostras são positivas; no gráfico, plotar a forma de onda.
   - Texto: `import math` / `t = 0.0` / `positivas = 0` / `while t <= 1.0:` / `    s = math.sin(2 * math.pi * 2 * t)` / `    if s > 0:` / `        positivas = positivas + 1` / `    t = t + 0.05` / `print(f"{positivas} amostras positivas")`
   - Gráfico: `import math` / `import matplotlib.pyplot as plt` / `ts = []` / `ss = []` / `t = 0.0` / `while t <= 1.0:` / `    ts.append(t)` / `    ss.append(math.sin(2 * math.pi * 2 * t))` / `    t = t + 0.05` / `plt.plot(ts, ss, marker=".")` / `plt.axhline(0, color="red", linestyle="--")` / `plt.title("Sinal amostrado")` / `plt.xlabel("t (s)")` / `plt.ylabel("amplitude")` / `plt.grid(True)` / `plt.show()`

(Sem teaser separado — o módulo 07 já tem soluções com gráfico de verdade.) Inserir, UMA vez (perto do primeiro bloco gráfico), a nota curta sobre listas previstas no módulo 9.

- [ ] **Step 1: Ler o arquivo atual e o molde 06** (`licoes/07-juntando-tudo.html`).
- [ ] **Step 2: Reescrever `licoes/07-juntando-tudo.html`** preservando os 3 projetos, seus callouts e a seção "E agora?"; inserindo analogia ⚙️, SVG dos blocos convergentes, "Na engenharia" (3 cards laço+condicional), 12 exercícios (4/5/3) com os 3 🔴 em solução dupla, e a nota sobre listas.
- [ ] **Step 3: Autorrevisão estática** — 12 exercícios (4/5/3), numeração 1–12; 3 🔴 com `.solucao-dupla` (texto + `data-mpl`/`import matplotlib`/`plt.show()`); soluções usam laços/condicionais/`random`/`math` mas SEM `def` nem listas (exceto `.append` para montar gráfico e a lista-literal do caixa eletrônico, ambas com nota); `axhline` com `linestyle="--"`; 3 cards; SVG theme-aware (sem `#hex`) com `aria-label`; conteúdo original (3 projetos + "E agora?") preservado; rótulos/abertura exatos; classes batem; `<`/`>` corretos; laços terminam.
- [ ] **Step 4: Commit**
```bash
git add licoes/07-juntando-tudo.html
git commit -m "feat: enriquece módulo 07 (juntando tudo) com engenharia, SVG e soluções com/sem gráfico

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review (autor do plano)

**Cobertura:** enriquecimento do capstone 07 com analogia, SVG, "Na engenharia", 12 exercícios graduados e — por ser pós-laços — soluções duplas (com/sem gráfico) nos 3 🔴 → Task 1. Preservação dos 3 projetos + "E agora?" exigida. ✔

**Placeholder scan:** cada exercício tem enunciado + solução de referência; os 3 🔴 têm os dois blocos completos; cards com código completo. ✔

**Consistência de pré-requisitos:** usa só até laços (06) + `random`/`math`; sem `def`; listas só via `.append` para gráfico e lista-literal do caixa eletrônico, ambas com nota de "prévia do módulo 9". `axhline` tracejado. Rótulos/abertura idênticos ao molde. SVG sem `#hex`. ✔

**Conclusão:** este é o último módulo do enriquecimento — após ele, TODOS os 14 módulos seguem o molde de engenharia.
